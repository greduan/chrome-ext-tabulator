Remove top-level IIFE from all JS files.
Use Webpack or some other bundling thing, to allow code reuse and simplifciation of logic inside one file.
Go through and clean/simplify current logic in all JS files.
Make compatible with all browsers (that support WebExtensions).
Remove features / streamline flows.
